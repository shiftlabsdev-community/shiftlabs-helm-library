{{- $_ := required "image.repository is required" .Values.image.repository -}}
{{- $_ := required "cronjob.schedule is required" .Values.cronjob.schedule -}}
{{- $fullName := include "cronjob.fullname" . -}}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "cronjob.fullname" . }}
  labels:
    {{- include "cronjob.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  schedule: {{ .Values.cronjob.schedule | quote }}
  {{- if .Values.cronjob.timeZone }}
  timeZone: {{ .Values.cronjob.timeZone | quote }}
  {{- end }}
  concurrencyPolicy: {{ .Values.cronjob.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.cronjob.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.cronjob.failedJobsHistoryLimit }}
  {{- if .Values.cronjob.startingDeadlineSeconds }}
  startingDeadlineSeconds: {{ .Values.cronjob.startingDeadlineSeconds }}
  {{- end }}
  suspend: {{ .Values.cronjob.suspend }}
  jobTemplate:
    metadata:
      labels:
        {{- include "cronjob.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.job.backoffLimit }}
      backoffLimit: {{ .Values.job.backoffLimit }}
      {{- end }}
      {{- if .Values.job.completions }}
      completions: {{ .Values.job.completions }}
      {{- end }}
      {{- if .Values.job.parallelism }}
      parallelism: {{ .Values.job.parallelism }}
      {{- end }}
      {{- if .Values.job.activeDeadlineSeconds }}
      activeDeadlineSeconds: {{ .Values.job.activeDeadlineSeconds }}
      {{- end }}
      {{- if .Values.job.ttlSecondsAfterFinished }}
      ttlSecondsAfterFinished: {{ .Values.job.ttlSecondsAfterFinished }}
      {{- end }}
      template:
        metadata:
          annotations:
            {{- if .Values.configmap.enabled }}
            checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
            {{- end }}
            {{- with .Values.podAnnotations }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          labels:
            {{- include "cronjob.selectorLabels" . | nindent 12 }}
            {{- with .Values.podLabels }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
        spec:
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          serviceAccountName: {{ include "cronjob.serviceAccountName" . }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          restartPolicy: {{ .Values.job.restartPolicy }}
          terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds | default 30 }}
          {{- if .Values.dnsPolicy }}
          dnsPolicy: {{ .Values.dnsPolicy }}
          {{- end }}
          {{- with .Values.dnsConfig }}
          dnsConfig:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.hostAliases }}
          hostAliases:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if .Values.priorityClassName }}
          priorityClassName: {{ .Values.priorityClassName }}
          {{- end }}
          {{- if .Values.runtimeClassName }}
          runtimeClassName: {{ .Values.runtimeClassName }}
          {{- end }}
          {{- if .Values.shareProcessNamespace }}
          shareProcessNamespace: {{ .Values.shareProcessNamespace }}
          {{- end }}
          {{- if or .Values.initContainers.enabled .Values.extraInitContainers }}
          initContainers:
            {{- if .Values.initContainers.enabled }}
            {{- range .Values.initContainers.containers }}
            - name: {{ .name }}
              image: {{ .image }}
              {{- if .command }}
              command: {{ .command | toJson }}
              {{- end }}
              {{- if .args }}
              args: {{ .args | toJson }}
              {{- end }}
              {{- if .env }}
              env:
                {{- toYaml .env | nindent 16 }}
              {{- end }}
              {{- if .volumeMounts }}
              volumeMounts:
                {{- toYaml .volumeMounts | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- end }}
            {{- with .Values.extraInitContainers }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
          containers:
            - name: {{ .Chart.Name }}
              securityContext:
                {{- toYaml .Values.securityContext | nindent 16 }}
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              {{- if .Values.command }}
              command:
                {{- toYaml .Values.command | nindent 16 }}
              {{- end }}
              {{- if .Values.args }}
              args:
                {{- toYaml .Values.args | nindent 16 }}
              {{- end }}
              {{- if or .Values.env .Values.configmap.enabled .Values.ExternalSecret.enabled .Values.extraEnvFrom }}
              {{- if .Values.env }}
              env:
                {{- toYaml .Values.env | nindent 16 }}
              {{- end }}
              envFrom:
              {{- if .Values.configmap.enabled }}
              - configMapRef:
                  name: {{ include "cronjob.fullname" . }}
              {{- end }}
              {{- if .Values.ExternalSecret.enabled }}
              - secretRef:
                  name: {{ include "cronjob.fullname" . }}
              {{- end }}
              {{- with .Values.extraEnvFrom }}
              {{- toYaml . | nindent 14 }}
              {{- end }}
              {{- end }}
              {{- if or .Values.volumeMounts (and .Values.configmap.enabled .Values.configmap.fileData) .Values.extraVolumeMounts }}
              volumeMounts:
              {{- if .Values.configmap.enabled }}
              {{- if .Values.configmap.fileData }}
              {{- range .Values.configmap.fileData }}
              - name: custom-config-volume-{{ .fileName | replace ".yaml" "" | replace ".yml" "" | replace ".json" "" | replace ".conf" "" }}
                mountPath: {{ .mountPath }}/{{ .fileName }}
                subPath: {{ .fileName }}
              {{- end }}
              {{- end }}
              {{- end }}
              {{- with .Values.volumeMounts }}
              {{- toYaml . | nindent 14 }}
              {{- end }}
              {{- with .Values.extraVolumeMounts }}
              {{- toYaml . | nindent 14 }}
              {{- end }}
              {{- end }}
              {{- with .Values.lifecycle }}
              lifecycle:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
          {{- with .Values.extraContainers }}
          {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if or .Values.volumes (and .Values.configmap.enabled .Values.configmap.fileData) .Values.extraVolumes }}
          volumes:
          {{- if .Values.configmap.enabled }}
          {{- if .Values.configmap.fileData }}
          {{- range .Values.configmap.fileData }}
          - name: custom-config-volume-{{ .fileName | replace ".yaml" "" | replace ".yml" "" | replace ".json" "" | replace ".conf" "" }}
            configMap:
              name: {{ $fullName }}
              items:
              - key: {{ .fileName }}
                path: {{ .fileName }}
          {{- end }}
          {{- end }}
          {{- end }}
          {{- with .Values.volumes }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
          {{- with .Values.extraVolumes }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
          {{- end }}
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.topologySpreadConstraints }}
          topologySpreadConstraints:
            {{- toYaml . | nindent 12 }}
          {{- end }}
